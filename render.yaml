services:
  - type: worker
    name: telegram-bot
    env: python
    startCommand: "python bot.py"
    buildCommand: "pip install -r requirements.txt"
    envVars:
      - key: BOT_TOKEN
        sync: false